using System;
using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;
using TurnUpPortal.Pages;

namespace TurnUpPortal.Tests
{
    public class EmployeesTest
    {
        public EmployeesTest(IWebDriver driver)
        {

            // Login page object initialization and definition

            /*LoginPage loginObject = new LoginPage();
            loginObject.LoginSteps(driver);

            // Home page initialization and definition

            HomePage homePageObject = new HomePage();
            homePageObject.NavigateToEmployees(driver);

            // Employees page object initialization and definition

            EmployeesPage employeesObject = new EmployeesPage();
            employeesObject.CreateEmployee(driver);

            // Edit existent Employee record

            employeesObject.EditEmployee(driver);

            // Delete existent Employee record

            employeesObject.DeleteEmployee(driver);



           */ 
        }
    }
}
